# Application dependencies

require:
    - play
    - customModules -> playsqlmodule
repositories:
    - playCustomModules:
           type:  local
           artifact: "D:/work/play-1.2.2/samples-and-tests/playsqlmodule/"
           contains:
                    - customModules ->  *